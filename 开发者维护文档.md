# App Create 开发者维护文档

## 1. 当前分发架构
采用“启动器 EXE + 在线主程序包（zip）”模式：

1. 用户安装 `AppCreateLauncherSetup-*.exe`（一次）。
2. 用户每次点桌面 `App Create`。
3. 启动器读取 `release_url.txt`，检查远端包指纹（ETag/Last-Modified/Length）。
4. 检测到新版本时弹窗让用户选择是否更新。
5. 用户确认更新后调用 `deploy_windows.ps1` 更新到 `C:\app-create`，再运行 `user_ops\run_windows.cmd`。

## 2. 关键路径
- 启动器逻辑：`launcher/AppCreateLauncher.ps1`
- 启动器入口：`launcher/AppCreateLauncher.cmd`
- 在线部署脚本：`deploy_windows.ps1`
- 启动器打包脚本：`tech_ops/build_launcher_installer.ps1`
- 主程序打包脚本：`package_release.ps1` / `package_release.sh`

## 3. 发布前准备
1. 准备一个稳定的 zip 下载地址（建议固定 URL）。
2. 把地址写入根目录 `release_url.txt` 第一条非注释行。

说明：启动器打包时优先读取根目录 `release_url.txt`，再回退 `launcher/release_url.txt`。

## 4. 标准发布流程
### A. 构建主程序 zip
```powershell
npm run release:zip:win
```
输出：
- `dist/app-create-<timestamp>.zip`
- `dist/app-create-latest.zip`

### B. 上传 zip 到服务器
把 `app-create-latest.zip` 上传并覆盖同一下载地址（推荐固定 URL 不变）。

### C. 是否需要重发启动器 EXE
1. 下载地址不变：不需要重发 EXE，用户下次启动会自动检测。
2. 下载地址变化 / 启动器逻辑变化：需要重打并重发启动器 EXE。

## 5. 构建启动器 EXE
```powershell
npm run release:launcher:exe:win -- -InnoCompilerPath "D:\Inno Setup 6\ISCC.exe"
```

临时覆盖下载地址：
```powershell
npm run release:launcher:exe:win -- -InnoCompilerPath "D:\Inno Setup 6\ISCC.exe" -PackageUrl "https://your-domain.com/app-create-latest.zip"
```

输出：`dist/AppCreateLauncherSetup-<version>-<timestamp>.exe`

## 6. 旧 EXE 清理策略
`build_launcher_installer.ps1` 默认会在每次打包前删除旧的 `AppCreateLauncherSetup*.exe`。

如需保留历史安装包：
```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tech_ops\build_launcher_installer.ps1 -KeepOldInstallers
```

## 7. 用户侧升级行为说明
1. 用户必须从桌面 `App Create` 启动，才能进入更新检查。
2. 检测到新版本时由用户选择：更新 / 跳过本次 / 取消启动。
3. 需要强制更新时，可手动执行启动器并加 `-ForceUpdate`。

## 8. 回滚方案
1. 保留历史版本 zip（例如 `app-create-20260511-xxxxxx.zip`）。
2. 将线上固定下载地址回指到旧 zip。
3. 用户下次启动时可选择更新并回滚到旧版本。

## 9. 常见故障排查
1. 启动器无法更新：检查 `release_url.txt` 是否可访问、TLS/防火墙是否放行。
2. VPS 无 Node 环境：由 `bootstrap_windows.ps1` 自动处理（优先内置 runtime，失败再在线安装）。
3. 快捷方式异常：重新运行一次部署，脚本会重建 `App Create` 桌面快捷方式。
4. 入口混淆：对外只给“桌面 App Create”这一入口。
