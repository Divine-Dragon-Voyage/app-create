# App Create 用户使用文档

## 1. 适用场景
你是最终使用者，只需要会双击运行，不需要手动安装 Node/npm。

## 2. 你会拿到什么
开发者会给你一个安装包：

- `AppCreateLauncherSetup-*.exe`

安装后桌面会有一个快捷方式：

- `App Create`

## 3. 首次安装
1. 双击 `AppCreateLauncherSetup-*.exe`，按提示完成安装。
2. 安装完成后，桌面会出现 `App Create`。
3. 双击 `App Create` 启动。

## 4. 每次运行流程
1. 双击桌面 `App Create`。
2. 弹出“应用创建启动器”窗口后，填写：
   - `开发者 ID 或链接`：可以填纯数字 ID，或完整 Play Console 开发者链接。
   - `Excel 文件路径`：选择本次要跑的 `.xlsx/.xls` 文件。
3. 点击 `开始运行`。
4. 首次运行或版本更新时，程序会自动检查更新并准备环境（可能需要几分钟）。
5. 浏览器打开后，如需登录请完成登录，然后脚本会继续执行。

## 5. Excel 基本要求
至少要有两列（列名支持中英文）：

- 应用名：`应用名称` / `应用名` / `App Name` / `Application Name`
- 包名：`应用包名` / `包名` / `App Package Name` / `Package Name` / `Application ID`

脚本会自动维护两列（如果没有会自动新增）：

- `status`
- `progress_step`

已完成（`status=DONE`）的行，重跑时会自动跳过。

## 6. 更新机制（你需要知道的）
1. 日常只用点桌面 `App Create`。
2. 启动器会按配置地址拉取最新程序包并更新。
3. 不要手动点 `run_windows.cmd` 作为日常入口，否则可能绕过更新检查。

## 7. 常见问题
1. 提示 Excel 不存在：重新选择正确的 `.xlsx/.xls` 文件。
2. 提示开发者链接不合法：确认是 `play.google.com` 且包含 `/developers/<id>`。
3. 黑窗一闪而过：请从桌面 `App Create` 启动，不要直接点内部脚本。
4. 页面布局偶尔异常（左侧栏缺失/空白）：通常是网络或页面加载问题，重试一次即可。

## 8. 建议
1. 运行期间不要频繁切换或关闭自动化浏览器窗口。
2. 同一批次跑完前，不要改动 Excel 列结构。

