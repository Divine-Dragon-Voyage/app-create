# 使用说明（Windows VPS）

## 1. 首次安装

1. 将项目放到 VPS（例如 `C:\app-create`）。
2. 进入 `user_ops` 目录，双击 `install_windows.cmd`。
3. 看到 UAC 弹窗时点击“是”。
4. 等待安装完成（会自动初始化 Node/npm/依赖并检查浏览器 CDP）。
5. 安装完成后会自动在桌面创建快捷方式：`App Create`。

## 2. 每次运行

1. 双击桌面快捷方式 `App Create`（或双击 `user_ops\run_windows.cmd`）。
2. 会弹出中文启动窗口，输入两项：
   - 开发者 ID 或链接
   - Excel 文件路径（`.xlsx` / `.xls`）
3. 点击“开始运行”后，脚本自动校验并开始任务。

## 3. 浏览器与登录

1. 脚本会优先连接 `9222` CDP。
2. 若未检测到可用 CDP，会按现有逻辑拉起可调试浏览器。
3. 若已在 Play Console 首页则直接开始；若是待登录状态则先登录再继续。

## 4. 更新版本

1. 双击 `user_ops\update_windows.cmd`。
2. 更新完成后继续用桌面快捷方式 `App Create` 运行。

## 5. 常见问题

1. 看不到弹窗：右键 `run_windows.cmd` 选择“以管理员身份运行”再试。
2. 提示 Excel 无效：确认路径存在且扩展名是 `.xlsx` 或 `.xls`。
3. 提示开发者链接无效：请填纯数字开发者 ID，或完整 Play Console 开发者链接。
4. 任务中断：把窗口最后 20 行日志发给技术人员排查。
