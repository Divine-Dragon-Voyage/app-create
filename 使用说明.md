1. 点击 `启动.cmd`。
2. 根据提示粘贴开发者链接或数字 ID，例如：
   `https://play.google.com/console/u/0/developers/123456789/app-list`
   或
   `123456789`
3. 粘贴或右侧选择 Excel 文件路径。
4. 运行时会自动维护 Excel 的 `status` 列（不存在会自动新增）。
5. `status = DONE` 表示该行已全部完成，重跑会自动跳过。
6. `status = PARTIAL` 表示该行应用已创建但未完结，重跑会按“应用名 + 包名”匹配并点击 `View app` 继续。
7. 脚本会自动维护 `progress_step` 细粒度断点列。
8. 每完成一个声明步骤会立即写入：
   `ADS_DONE / APP_ACCESS_DONE / AUDIENCE_DONE / AD_ID_DONE / GOV_DONE / FINANCE_DONE / HEALTH_DONE / COUNTRY_DONE / DONE`
9. 重跑时优先按 `progress_step` 续跑，不会总是从 Ads 开始。
10. 如果 `progress_step` 缺失或偏旧，脚本会读取页面声明卡片状态（`Start/非Start`）自动校准进度后再继续。
